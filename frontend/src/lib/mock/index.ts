/**
 * MOCK DATA BOUNDARY
 * ------------------
 * All exports from this file are placeholder data only.
 * They must be replaced by real API calls before production.
 * Components using these exports must be clearly marked: [MOCK] or import from this module explicitly.
 *
 * Current mock scope: FE-01 stubs (shells only — no mock data yet)
 * Real API adapters: src/lib/api/*
 */

export const MOCK_BOUNDARY_VERSION = '0.1.0'
