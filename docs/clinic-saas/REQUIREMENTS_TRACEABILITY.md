# MetoCare Clinic SaaS — Requirements Traceability Matrix

One row per backend-enforced functional requirement (`BR-Mxx-nn`, plus the M12 `GAP-Rn` rule catalog and M14's `AI-0n` capability codes, since the BRD itself treats each as independently testable). Source: `docs/brd/v2.0/*.md` (authoritative), cross-checked against `docs/brd/v1.0/executive-brd.md`.

API/UI/Test/Phase columns are this analyst's inference — no codebase was consulted (per task scope) — and are prefixed `likely:` where speculative. Phase follows the program's C0/C1/C2 scope definition given for this task (not the BRD's own C0–C4 phase labels, which are noted in parentheses where they diverge).

| BRD Requirement ID | Requirement (short) | Source Module | Domain | Proposed API surface | Proposed UI surface | Test coverage needed | Phase |
|---|---|---|---|---|---|---|---|
| BR-M01-01 | Every record scoped by `clinic_id`; server ignores client-supplied tenant id | M01 | Tenant | likely: all endpoints via `TenantContext` dependency injection, no `clinic_id` in request body honored | n/a (backend invariant) | Cross-tenant negative test: user A + resource of tenant B → 403/404 | C0 |
| BR-M01-02 | Suspended/Expired tenant blocks all writes, allows minimal reads, no deletion | M01 | Tenant | likely: `PATCH /admin/tenants/{id}/status`; write-path guard middleware | likely: Admin banner "Tenant suspended", disabled action buttons | Attempt write while Suspended → controlled error; data still readable after reactivation | C0 |
| BR-M01-03 | Deactivated is terminal; restore needs platform approval + audit | M01 | Tenant | likely: `POST /admin/tenants/{id}/restore` (Platform Admin only) | likely: Platform console restore-request flow | Non-platform actor cannot restore; restore event fully audited | C0 |
| BR-M01-04 | Legal-info changes (name, MST, license) Owner-only + audited old/new | M01 | Tenant | likely: `PATCH /clinics/{id}/legal-info` | likely: Clinic Settings → Legal Info form | Non-Owner blocked (403); audit record has before/after | C0 |
| BR-M01-05 | Reminder/branding content must not embed diagnosis or extra PHI | M01 | Tenant / Notifications | likely: template content validator in `POST /clinics/{id}/branding` | likely: branding editor with content-policy hint | Automated PHI-pattern scan on saved templates | C0 |
| BR-M02-01 | `branch_id` derived from authenticated membership/session, not trusted from client | M02 | Branch | likely: `TenantContext`/`BranchContext` dependency resolves branch from membership | n/a (backend invariant); branch switcher UI only offers valid set | Call any branch-scoped API with a `branch_id` outside membership → 403 | C0 |
| BR-M02-02 | No booking outside branch working hours unless overridden with audited reason | M02 | Branch / Appointment | likely: `POST /appointments` validates against branch `working_hours` | likely: booking UI grays out out-of-hours slots, override requires reason modal | Booking outside hours rejected; override path requires reason + creates audit record | C1 (branch config itself is C0) |
| BR-M02-03 | Paused branch blocks new bookings; existing bookings flagged for reschedule | M02 | Branch | likely: `PATCH /branches/{id}/status` | likely: Paused-branch banner + affected-appointments list for reception | Booking attempt at Paused branch rejected; historical bookings unaffected | C0 |
| BR-M02-04 | No hard delete of a branch with history; Paused/Archived only | M02 | Branch | likely: `DELETE /branches/{id}` disabled once history exists, only `PATCH status=Archived` | likely: delete button hidden/disabled with tooltip once branch has data | Attempt hard delete on branch with appointments → rejected | C0 |
| BR-M03-01 | Every endpoint checks membership.roles × resource × action against Appendix A | M03 | RBAC | likely: shared `require_permission(resource, action)` FastAPI dependency | n/a (backend invariant); frontend hides but never relies on hiding | Per-role positive + negative (403) test matrix mirroring Appendix A | C0 |
| BR-M03-02 | Multi-clinic doctor: independent membership/schedule/patient-list per clinic, no cross-clinic query | M03 | RBAC / Staff | likely: all doctor-scoped queries filtered by active `clinic_id` from session, never global doctor id alone | likely: clinic switcher for multi-clinic doctors | Doctor in 2 clinics, switch context, assert zero data bleed either direction | C0 |
| BR-M03-03 | Suspending membership revokes access ≤60s (incl. token revocation); authored data retained with original attribution | M03 | RBAC / Staff | likely: `PATCH /clinics/{id}/memberships/{id}/status` + token/session revocation hook | likely: staff list "Suspend" action with confirmation | Suspend then replay old token within 60s window → must fail by t+60s; authored records still show original author | C0 |
| BR-M03-04 | Cannot demote/lock the tenant's last active Owner | M03 | RBAC / Staff | likely: guard in membership-update service layer | likely: disabled "Remove"/"Change role" control on sole Owner with explanatory tooltip | Attempt to suspend/demote last Owner → controlled error, not a 500 | C0 |
| BR-M03-05 | Doctor role requires a doctor profile (name, license no., specialty); clinic verifies | M03 | Staff | likely: `POST /clinics/{id}/staff/invite` requires `doctor_profile_ref` when role=Doctor | likely: invite-staff form conditionally requires doctor profile fields | Invite Doctor without profile fields → validation error | C0 |
| BR-M03-06 | Least-privilege: Receptionist/Accountant get zero clinical API surface; Care Coordinator gets only M13 care-context | M03 | RBAC | likely: response-schema-level field filtering per role, not just route-level | n/a | Schema-level test: no clinical field appears in any Receptionist/Accountant response body | C0 |
| BR-M04-01 | Over-limit request → HTTP 403 `ENTITLEMENT_EXCEEDED` + Vietnamese message | M04 | Subscription/Entitlement | likely: `check_entitlement(feature)` dependency raising a typed exception mapped to 403 | likely: upsell modal "Nâng cấp gói để dùng tính năng này" | Basic plan calls Copilot endpoint → 403 with exact error code | C0 |
| BR-M04-02 | Downgrade never deletes over-limit data; it becomes read-only | M04 | Subscription/Entitlement | likely: entity-level `is_read_only` computed from current plan vs. usage | likely: read-only badge/banner on over-limit branches/records | Downgrade with 3 branches on a 1-branch plan → branches 2/3 read-only, still visible | C0 |
| BR-M04-03 | Trial expiry → Expired per M01 state machine; warnings at 7/3/1 days | M04 | Subscription / Tenant | likely: scheduled job `check_trial_expiry` + `POST /notifications/trial-warning` | likely: in-app countdown banner during Trial | Trial clock mocked to T-7/-3/-1/0 → correct warning + correct terminal state | C0 |
| BR-M04-04 | Copilot usage counted per successful AI call; quota shown to doctor | M04 | Subscription / AI | likely: `GET /doctor/copilot/quota`; increment on successful `POST /copilot/*` call only | likely: quota indicator in Copilot panel | Failed AI call does not decrement quota; successful call does | C3 (beyond C0–C2 scope; Copilot itself is out of C0) |
| BR-M05-01 | Price changes non-retroactive; bookings/invoices snapshot price at creation | M05 | Services/Pricing | likely: `price_snapshot` column copied onto `appointment`/`invoice_line` at create time | n/a (data invariant) | Change service price after booking exists → invoice for that booking still uses old price | C1 |
| BR-M05-02 | All price changes audited (actor, old→new) | M05 | Services/Pricing | likely: audit hook on `PATCH /services/{id}` | likely: price-history view on service detail page | Price change produces audit record with before/after values | C1 |
| BR-M05-03 | Cannot deactivate a service with future bookings without warning + affected list | M05 | Services/Pricing | likely: `PATCH /services/{id}/status` returns 409 with affected-appointments payload unless force-confirmed | likely: confirmation modal listing affected future appointments | Deactivate service with future bookings → warning surfaced, not silently applied | C1 |
| BR-M05-04 | Package-benefit usage decrements correctly; exhausted benefits fall back to itemized billing with reception confirmation | M05 | Services/Pricing / Billing | likely: `POST /patients/{id}/packages/{id}/consume` | likely: "gói còn X lượt" indicator + fallback confirmation dialog at checkout | 6-visit package, 7th visit → warning + itemized billing path exercised | C1 |
| BR-M06-01 | Patient records never hard-deleted; Inactive/Merged only | M06 | Patient | likely: `DELETE /patients/{id}` disabled; only `PATCH status=Inactive` | likely: no hard-delete button anywhere in patient admin UI | Attempt hard delete via API → rejected; record remains queryable as Inactive | C1 |
| BR-M06-02 | Cross-clinic: clinic sees only data it created + consent-shared data, default no sharing | M06 | Patient / Consent | likely: patient-record query joins consent grants scoped to requesting `clinic_id` | n/a (backend invariant) | Patient seen at clinic A and B; B without consent sees zero A-created clinical data | C1 |
| BR-M06-03 | Receptionist has zero API access to clinical content | M06 | RBAC / Patient | likely: response schema filtering as in BR-M03-06 | n/a | Receptionist token calling any note/lab/diagnosis endpoint → 403 | C1 |
| BR-M06-04 | Patient code auto-generated, unique-in-tenant, immutable | M06 | Patient | likely: sequence/generator scoped by `clinic_id` on `POST /patients` | likely: read-only patient-code field in patient profile | Two patients in same tenant never get duplicate codes; code field non-editable after create | C1 |
| BR-M06-05 | Manual lab entry requires standard biomarker+value+unit+date; unit mismatch warns, never auto-converts | M06 | Patient | likely: `POST /patients/{id}/labs` validates against biomarker reference catalog | likely: lab-entry form with catalog-driven biomarker picker + unit warning | Enter mismatched unit → warning shown, value stored as-entered, no silent conversion | C1 |
| BR-M06-06 | Patient list must paginate; no endpoint returns full dataset | M06 | Patient | likely: `GET /patients?page=&limit=` with enforced max page size | likely: paginated table/infinite scroll in patient list UI | Request without pagination params still returns bounded page, not entire tenant dataset | C1 |
| BR-M06-07 | National ID stored only opt-in with legal basis; encrypted + masked | M06 | Patient | likely: `national_id` column encrypted at rest, feature-flagged per tenant | likely: masked display (`***-***-1234`), reveal-on-permission | Tenant without opt-in cannot store national ID; stored value never returned unmasked to unauthorized roles | C1 |
| BR-M07-01 | Appointment transitions restricted to state machine; all transitions audited | M07 | Appointment | likely: `PATCH /appointments/{id}/status` validated against a transition table | likely: status-change buttons only show valid next-states | Full transition-table test: every valid pass, every invalid rejected + audited | C1 |
| BR-M07-02 | No double-booking for one doctor unless controlled overbooking enabled with % cap | M07 | Appointment | likely: `POST /appointments` acquires a slot lock / unique constraint on (doctor, time range) | likely: booking UI disables already-booked slots | Two concurrent booking requests for same doctor/slot → exactly one succeeds | C1 |
| BR-M07-03 | Booking must respect branch + doctor working hours; override needs permission + reason | M07 | Appointment / Branch | likely: same validator as BR-M02-02, doctor-schedule variant | likely: override requires elevated-role reason modal | Booking outside doctor's declared hours rejected unless permitted override w/ reason | C1 |
| BR-M07-04 | Cancellation inside cancellation-policy window flagged for reporting | M07 | Appointment | likely: `PATCH /appointments/{id}/status=Cancelled` checks `cancellation_policy.min_hours_before` | likely: policy-violation flag surfaced in cancellation report | Cancel inside window → flagged record appears in policy-violation report | C1 |
| BR-M07-05 | Idempotent end-of-day no-show job; configurable grace period (default 60 min) | M07 | Appointment | likely: scheduled job `mark_no_shows(branch_id)`, idempotency key per (branch, date) | n/a (background job) | Run job twice for same day → no duplicate status changes or duplicate Care Gap tasks | C1 |
| BR-M07-06 | Patient self-booking sees only tenant-opened online slots; never other patients' names | M07 | Appointment | likely: `GET /patient/booking/slots` returns only `online_booking_enabled=true` slots, anonymized | likely: patient app slot picker | Patient-facing slot list never includes another patient's identity in any field | C1 |
| BR-M08-01 | Check-in transitions appointment to Arrived→In queue; walk-in creates matching appointment | M08 | Queue | likely: `POST /checkin` (by appointment id or walk-in patient id) | likely: front-desk check-in screen (search by phone/patient code) | Check-in with valid appointment updates status chain correctly; walk-in creates a synthetic appointment record | C1 |
| BR-M08-02 | System never self-determines emergency; priority flag is human-entered, audited; clinical rules only suggest | M08 | Queue | likely: `PATCH /queue/{id}/priority` requires `reason` field | likely: priority-elevation control requires reason input | Priority elevation without reason rejected; elevation appears in audit log | C1 |
| BR-M08-03 | Queue numbers scoped/reset per tenant config; no duplicates within reset scope | M08 | Queue | likely: `queue_config`-driven number generator scoped by (tenant/branch/day) | likely: queue number displayed on check-in confirmation | Concurrent check-ins never produce duplicate queue numbers within same reset scope | C1 |
| BR-M08-04 | No-show-at-call returns patient to queue with missed-call flag, capped retries | M08 | Queue | likely: `POST /queue/{id}/call` + `POST /queue/{id}/no-response` | likely: "gọi nhỡ" flag badge in queue list | Repeated missed calls beyond configured max → reception-required manual resolution triggered | C1 |
| BR-M08-05 | Wait time measured check-in→In consultation; stored for reporting | M08 | Queue / Dashboard | likely: timestamp diff persisted on queue-entry record, read by `GET /reports/wait-time` | likely: average wait-time tile on operational dashboard | Wait-time value in report matches (consultation_start − checkin_time) for seeded data | C1 |
| BR-M09-01 | Finalized note is immutable; corrections only via amendment, full version chain visible | M09 | Encounter/Notes | likely: `PUT/DELETE /notes/{id}` rejected once `status=Finalized`; `POST /notes/{id}/amendments` | likely: note viewer shows version/amendment chain, no edit controls on finalized notes | Direct update/delete on finalized note → rejected; amendment creates new linked record, chain displays fully | C1 |
| BR-M09-02 | Only Doctor finalizes notes / writes Assessment-Plan; Nurse cannot | M09 | Encounter/Notes / RBAC | likely: field-level write permission on `assessment`/`plan`, `POST /notes/{id}/finalize` restricted to Doctor role | likely: Assessment/Plan fields read-only or hidden for Nurse role | Nurse account attempting to write Assessment/Plan or finalize → 403 | C1 |
| BR-M09-03 | Doctor can only open encounters for in-scope patients | M09 | Encounter/Notes / RBAC | likely: `POST /encounters` validated against doctor's schedule/queue/admin-assignment | n/a | Doctor A requests Doctor B's out-of-scope encounter → 403/404 | C1 |
| BR-M09-04 | Encounter links to an appointment or flagged walk-in; max one primary encounter per appointment | M09 | Encounter/Notes | likely: unique constraint on (appointment_id, is_primary=true) | n/a | Second primary-encounter creation attempt on same appointment → rejected | C1 |
| BR-M09-05 | AI/Copilot content never auto-written to a note; doctor must insert/accept; AI-sourced text marked | M09 | Encounter/Notes / AI | likely: `POST /notes/{id}/insert-ai-content` requires explicit doctor action, sets `source=ai` flag on the inserted segment | likely: visually distinct "AI-inserted" styling in note editor | Note content never appears from AI without an explicit insert action; AI-origin flag present on inserted segments | C3 (beyond C0–C2 scope; the note itself is C1, but AI insertion depends on Copilot) |
| BR-M09-06 | Lab orders in Plan auto-create a "not yet performed" tracking item feeding GAP-R2 | M09 | Encounter/Notes / Care Gap | likely: `POST /notes/{id}/finalize` side-effect creates `lab_order_tracking` row | likely: "Chưa thực hiện" list surfaced on patient profile | Finalizing a note with a lab order creates a trackable pending-lab record visible to Care Gap engine | C2 (tracking item is created in C1 but only consumed once Care Gap/M12 exists in C2) |
| BR-M10-01 | Locked invoice not editable by Receptionist; all adjustments audited with before/after + reason | M10 | Billing | likely: `PATCH /invoices/{id}/lines` rejected once `status≥Paid`; `POST /invoices/{id}/refund` | likely: locked-invoice UI shows line items read-only, refund as separate action | Attempt line edit on Paid invoice → rejected; refund creates audited record | C1 |
| BR-M10-02 | Line price from M05 snapshot; manual discount role-based cap (e.g. Reception ≤10%, Admin ≤30%) | M10 | Billing | likely: discount validator reading role-configured cap; over-cap routes to approval | likely: discount field with role-aware max + "request approval" flow | 15% discount by 10%-cap reception role → requires approval or rejected | C1 |
| BR-M10-03 | One encounter/appointment links to at most one primary invoice | M10 | Billing | likely: unique constraint on (encounter_id, is_primary=true) | n/a | Second primary-invoice creation for same encounter → rejected or routed to secondary invoice | C1 |
| BR-M10-04 | Package benefit usage creates 0-price line item + decrements benefit balance | M10 | Billing / Services | likely: shared logic with BR-M05-04's consume endpoint | likely: 0-price line item labeled "Sử dụng gói" on invoice | Using a package visit produces a $0 line + correct balance decrement | C1 |
| BR-M10-05 | Invoice numbers sequential per tenant/branch, never reused after cancellation | M10 | Billing | likely: DB sequence scoped per (tenant[, branch]) — **scope ambiguous, needs architecture decision (see BRD_ANALYSIS Finding 7)** | n/a | Cancel an invoice, create a new one → new number never reuses the cancelled number | C1 |
| BR-M11-01 | Only Doctor creates/edits/activates care plan; one active plan per disease program; edits version | M11 | Care Plan | likely: `POST /care-plans`, `PATCH /care-plans/{id}` restricted to Doctor; versioning via append-new-row | likely: care-plan editor with version history tab | Doctor edit creates new version, old version read-only; non-Doctor blocked | C2 |
| BR-M11-02 | Patient sees only doctor-published content, never draft/internal notes | M11 | Care Plan | likely: `GET /patient/care-plan` filters `status=Published` only | likely: patient-app care-plan view | Draft-only content never appears via patient-facing endpoint | C2 |
| BR-M11-03 | Each item auto-derives status (Done/Not-done/Overdue/Skipped) and emits events to M12 | M11 | Care Plan / Care Gap | likely: scheduled job evaluates due dates, emits `care_plan_item.overdue` event | likely: status badges on care-plan item list | Item due-date passes → status flips + Care Gap Queue receives event | C2 |
| BR-M11-04 | Alert thresholds are deterministic rules generating high-priority Care Gaps, never auto-messaging patients | M11 | Care Plan / Care Gap | likely: threshold-evaluation service, output routed only to M12, never to M15 directly | n/a | Threshold breach creates a Care Gap task requiring human confirmation; zero automatic patient-facing message with medical content | C2 |
| BR-M11-05 | Tenant-level template library, cloneable from MetoCare template library (P2) | M11 | Care Plan | likely: `GET /care-plan-templates`, `POST /care-plans/from-template/{template_id}` | likely: "Tạo từ mẫu" picker in care-plan creation flow | Creating from template pre-fills expected fields correctly | C2 |
| GAP-R1 | Overdue follow-up (≥3 days past due, configurable) | M12 | Care Gap | likely: rule evaluated in nightly `care_gap_detection` job | likely: Care Gap Queue list item "Quá hạn tái khám" | Patient with follow-up 3+ days overdue → task created with "Cần xử lý sớm" priority | C2 |
| GAP-R2 | Overdue lab order (≥7 days past due) | M12 | Care Gap | likely: consumes M09's lab-order tracking record (BR-M09-06) | likely: task reason "Chưa thực hiện xét nghiệm" | Lab order 7+ days overdue → task created | C2 |
| GAP-R3 | Medication running out within ≤7 days, no follow-up booked | M12 | Care Gap | likely: estimated-run-out-date computed from Care Plan medication fields | likely: task reason "Sắp hết thuốc" | Simulated run-out date within 7 days and no appointment → task created | C2 |
| GAP-R4 | Worsening biomarker vs. care-plan threshold or N-reading trend | M12 | Care Gap | likely: consumes BR-M11-04 threshold events; realtime (not nightly-only) | likely: task reason "Chỉ số xấu đi" with Emergency/Urgent priority | Threshold-breaching lab result → immediate task, not waiting for nightly batch | C2 |
| GAP-R5 | Lost to follow-up: no interaction ≥90 days for an "active" chronic patient | M12 | Care Gap | likely: nightly scan of last-interaction timestamp — **"active patient" definition needs resolving first (see BRD_ANALYSIS Finding 4)** | likely: task reason "Bỏ theo dõi" | 90+ days no interaction on a program patient → task created | C2 |
| GAP-R6 | No-show appointment | M12 | Care Gap / Appointment | likely: triggered directly off BR-M07-05's no-show job | likely: task reason "No-show" | No-show status transition → immediate Care Gap task | C2 |
| GAP-R7 | Chronic diagnosis, no care plan after 2 visits | M12 | Care Gap / Care Plan | likely: nightly scan joining diagnosis + encounter count + care-plan existence | likely: task reason "Chưa có care plan" | Patient w/ 2+ chronic-diagnosis encounters and no care plan → task created | C2 |
| GAP-R8 | Missing key program data (e.g. no HbA1c in 6 months for diabetes program) | M12 | Care Gap | likely: program-specific data-completeness rule set | likely: task reason "Dữ liệu thiếu" | Diabetes-program patient missing required biomarker window → task created | C2 |
| GAP-R9 | Doctor-requested manual follow-up | M12 | Care Gap | likely: `POST /care-gap-tasks` manual creation by Doctor from encounter/note | likely: "Tạo follow-up" button in note/encounter view | Doctor-created follow-up appears in queue with doctor-set priority | C2 |
| BR-M12-01 | Priority is rule-deterministic; AI cannot change urgency; manual override needs reason+audit | M12 | Care Gap | likely: `PATCH /care-gap-tasks/{id}/priority` requires `reason`; AI service has no write path to this field | likely: priority field with override-reason modal | Attempt AI-driven priority change → no such code path exists (negative/architecture test); manual override requires reason | C2 |
| BR-M12-02 | Dedupe: concurrent matching rules for one patient → one multi-reason task at highest priority | M12 | Care Gap | likely: detection job upserts by (patient_id, open task) rather than inserting per rule | n/a | Patient matching GAP-R1 + GAP-R3 same day → exactly one task with two reasons | C2 |
| BR-M12-03 | Rule engine idempotent — re-run never duplicates a task for same (patient, rule, cycle) | M12 | Care Gap | likely: idempotency key (patient_id, rule_id, cycle_period) on task creation | n/a | Run nightly job twice → task count unchanged | C2 |
| BR-M12-04 | Overdue tasks escalate to Admin; "Urgent" tasks must be claimed within ≤4 business hours | M12 | Care Gap | likely: scheduled escalation job + SLA timer per task | likely: escalation banner/notification for Admin | Urgent task unclaimed past 4 business hours → escalation event fires (exact Admin-side consequence still undefined per BRD_ANALYSIS Finding — flag for architecture) | C2 |
| BR-M12-05 | "Booked" outcome must link to a real appointment | M12 | Care Gap / Appointment | likely: `PATCH /care-gap-tasks/{id}/outcome=booked` requires `appointment_id` | likely: in-task "Đặt lịch" action creates linked appointment | Setting outcome=Booked without a linked appointment → rejected | C2 |
| BR-M12-06 | Revoking care-program consent closes open tasks, stops new ones (except doctor-decided safety exceptions) | M12 | Care Gap / Consent | likely: consent-revocation webhook closes tasks where `consent_scope=C5` | n/a | Revoke C5 → open tasks auto-close with defined outcome; no new tasks generated except doctor-flagged safety cases | C2 |
| BR-M13-01 | Care Coordinator API strictly whitelists care-context fields | M13 | CRM | likely: `GET /care-coordinator/worklist` uses a dedicated response schema, never the full patient/note schema | likely: call-list screen | Schema test: zero clinical fields (note/lab/diagnosis) present in worklist response | C2 |
| BR-M13-02 | Every call logs timestamp/caller/outcome/notes/created-appointment | M13 | CRM | likely: `POST /care-coordinator/tasks/{id}/log-call` | likely: in-call logging form | Call log record contains all required fields after submission | C2 |
| BR-M13-03 | Call scripts rule-configured, no medical detail beyond scope | M13 | CRM | likely: script templates keyed by GAP rule code | likely: script panel shown alongside call-list entry | Script content review confirms no medical detail beyond configured scope | C2 |
| BR-M13-04 | Max contact attempts per task (default 3) before forced closing outcome | M13 | CRM | likely: attempt counter on task, UI/API blocks further "no outcome" logging past cap | likely: attempt counter + forced outcome selector at cap | 4th contact attempt without a closing outcome → blocked/forced | C2 |
| BR-M13-05 | Configurable calling-hours window (default 8:30–19:30), warns on out-of-window logging | M13 | CRM | likely: tenant-config field, warning surfaced on `log-call` outside window | likely: soft warning banner in call-logging UI | Log a call outside configured window → warning shown, log still accepted | C2 |
| AI-01 | Pre-visit briefing (fixed structure incl. missing-data section, sourced) | M14 | AI/Copilot | likely: `POST /copilot/briefing` | likely: pre-visit briefing panel in doctor encounter view | Structured JSON schema validated; missing-data section present when applicable | C3 (beyond C0–C2 scope) |
| AI-02 | Case analysis: conflicts, differentials-as-questions, source+confidence | M14 | AI/Copilot | likely: `POST /copilot/case-analysis` | likely: "Phân tích bệnh cảnh" panel | Seeded conflicting data (e.g. stopped-but-active med) surfaces as a flagged conflict | C3 (beyond C0–C2 scope) |
| AI-03 | Suggested history-taking questions grouped by category | M14 | AI/Copilot | likely: `POST /copilot/suggested-questions` | likely: collapsible question groups in briefing panel | Output grouped correctly by symptom/progression/meds/lifestyle/warning-signs | C3 (beyond C0–C2 scope) |
| AI-04 | Suggested counseling content, doctor-inserted, AI-origin tagged | M14 | AI/Copilot / Encounter | likely: `POST /copilot/counseling-suggestions`, insertion via BR-M09-05's insert endpoint | likely: "Chèn vào note" action with AI-origin styling | Inserted content is tagged AI-origin in the note (ties to BR-M09-05 test) | C3 (beyond C0–C2 scope) |
| BR-M14-01 | §14.3 safety table (deterministic risk, consent gate, no-PHI-logs, schema validation, human confirm, feature flag OFF default, timeout, quota) is a go-live blocker | M14 | AI/Copilot | likely: composite of dependencies below, no single endpoint | n/a | Full safety-table checklist test suite, each row independently verifiable | C3 (beyond C0–C2 scope; this row underlies AC-M14-01..05) |
| BR-M14-02 | Copilot Doctor-only, in-scope-patient-only | M14 | AI/Copilot / RBAC | likely: same scope check as BR-M09-03 applied to `/copilot/*` routes | likely: Copilot entry point hidden for non-Doctor roles | Non-doctor role or out-of-scope patient → 403 on any `/copilot/*` call | C3 (beyond C0–C2 scope) |
| BR-M14-03 | Mandatory disclaimer on every AI output block | M14 | AI/Copilot | likely: disclaimer text embedded in every Copilot response payload/UI render | likely: fixed disclaimer footer on every Copilot panel | Every Copilot UI surface renders the disclaimer; cannot be dismissed permanently | C3 (beyond C0–C2 scope) |
| BR-M14-04 | Prompt/model versioning; production changes need approval + record | M14 | AI/Copilot | likely: `prompt_version`/`model_version` fields logged per call; change-management process outside the API itself | likely: Platform Admin "AI config" screen showing current version + change history | Model/prompt change without recorded approval → blocked by process (organizational control, not purely technical) | C3 (beyond C0–C2 scope) |
| BR-M15-01 | External-channel content (SMS/Zalo/Email) never contains diagnosis/labs/meds | M15 | Notifications | likely: template content-policy validator, same class of check as BR-M01-05 | likely: template editor with policy warnings | Automated PHI-pattern scan across all outbound-channel templates | C1 |
| BR-M15-02 | Channel consent (M17 C4) respected; disabled channel gets nothing except minimum legal notices | M15 | Notifications / Consent | likely: `send_notification()` service checks consent before dispatch per channel | n/a | Disable Email consent → zero emails sent except a defined mandatory-notice allowlist | C1 |
| BR-M15-03 | Every send logs template/channel/recipient/timestamp/status/error-code, bounded retry | M15 | Notifications | likely: `notification_log` table + `GET /admin/notifications/{id}` | likely: delivery-status view per appointment/patient | Failed send retried within bound, then marked Failed with error code | C1 |
| BR-M15-04 | Idempotency — one trigger never double-sends (dedupe key per event) | M15 | Notifications | likely: dedupe key = (event_id, channel, recipient) | n/a | Re-firing the same trigger event twice → single send recorded | C1 |
| BR-M15-05 | Marketing-message frequency cap per patient/week (P2) | M15 | Notifications | likely: rate-limit check before marketing-category sends | likely: config field in clinic notification settings | Exceeding weekly cap → additional marketing sends suppressed | C2 |
| BR-M16-01 | RBAC-respecting dashboards — Accountant no clinical view, Doctor defaults to own numbers | M16 | Dashboard | likely: `GET /reports/clinical` role-gated; `GET /reports/business` role-gated with doctor-scoping | likely: role-conditional dashboard sections | Accountant hitting clinical dashboard route → 403; Doctor sees own-scope numbers by default | C1 |
| BR-M16-02 | Exports role-gated; no bulk PHI export for unauthorized roles; every export audited | M16 | Dashboard | likely: `POST /reports/export` checks export permission + logs filter/row-count | likely: export button disabled/hidden per role | Unauthorized export blocked; authorized export produces an audit record with filters+count | C1 |
| BR-M16-03 | Clinical dashboard only aggregates consented/in-scope data | M16 | Dashboard / Consent | likely: aggregation query joins consent scope same as BR-M06-02 | n/a | Cohort figures exclude non-consented patient data | C1 |
| BR-M16-04 | Heavy aggregation via background job/materialized view; dashboard loads <3s | M16 | Dashboard | likely: scheduled materialized-view refresh, dashboard reads pre-aggregated tables | likely: "last updated" timestamp on dashboard | Load-time benchmark <3s at a stated dataset size (size TBD — see BRD_ANALYSIS Finding 10) | C1 |
| BR-M16-05 | Cohorts <5 patients anonymized (P2) | M16 | Dashboard | likely: aggregation query suppresses/aggregates further when group size <5 | likely: "Dữ liệu không đủ để hiển thị" placeholder for small cohorts | Cohort of 3 patients → suppressed/generalized, not shown as raw group of 3 | C2 |
| BR-M17-01 | Every consent record has scope/version/grant-time/channel/expiry/status, immutable evidence | M17 | Consent | likely: `POST /patients/{id}/consents`, append-only `consent_grant` table | likely: consent management screen (patient app + counter-collection UI) | Consent record retains full field set; historical grant records never mutated | C0 |
| BR-M17-02 | Revocation immediate for future processing; never deletes past audit/legal records | M17 | Consent | likely: `PATCH /patients/{id}/consents/{id}/revoke` | likely: "Thu hồi" action in patient consent settings | Revoke → immediate effect on new requests; historical audit/business records untouched | C0 |
| BR-M17-03 | AI gateway checks C3 pre-call; notification service checks C4 pre-send | M17 | Consent / AI / Notifications | likely: shared `check_consent(patient_id, scope)` helper called from both services | n/a | Missing C3 → Copilot call blocked; missing C4(channel) → that channel's send blocked | C0 (consent check itself; consuming services are C1/C3) |
| BR-M17-04 | Counter-collected consent needs witness field, re-confirmed on app activation | M17 | Consent | likely: `POST /patients/{id}/consents` with `channel=counter`, `witness_name` required | likely: front-desk consent capture form + re-confirmation prompt on first app login | Counter-collected consent missing witness → validation error; app-activation flow re-surfaces consent for confirmation | C0 |
| BR-M17-05 | VN personal-data-protection law compliance (incl. sensitive health data) — self-flagged as pending legal review | M17 | Consent | n/a — legal/compliance workstream, not purely an engineering deliverable | n/a | Legal sign-off artifact/checklist, not a code test | C0 (blocking item, open — see BRD_ANALYSIS Finding 9) |
| BR-M18-01 | Audit log append-only; captures actor/action/resource/tenant/time/IP/before-after | M18 | Audit | likely: dedicated `audit_log` table with no UPDATE/DELETE grants at the DB role level, write-only service | n/a | Attempt to modify/delete an audit record via any API path → rejected at both app and DB-role level | C0 |
| BR-M18-02 | Audit content excludes PHI beyond necessary (reference IDs, not note content) | M18 | Audit | likely: audit-writer serializer whitelists fields, never dumps raw note/lab content | n/a | Automated PHI-pattern scan across audit_log table finds zero note/lab content | C0 |
| BR-M18-03 | Audit query access role-scoped (Owner: own tenant; Platform Admin: ops, not clinical by default) | M18 | Audit | likely: `GET /clinics/{id}/audit` (Owner-scoped), `GET /admin/audit` (Platform, ops-only view) | likely: audit log viewer per role | Owner cannot query other tenants' audit; Platform Admin default view excludes clinical-content audit entries | C0 |
| BR-M18-04 | Retention period self-flagged unresolved ("proposed ≥5 years, pending legal review") | M18 | Audit | likely: `audit_log` partitioning/retention policy config, value TBD | n/a | Retention-policy config test once the legal-review number is finalized (currently blocked — see BRD_ANALYSIS Finding 9) | C0 (blocking item, open) |
