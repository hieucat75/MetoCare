# Metabolic Care Platform — backend runtime dependencies (MVP foundation)
# Pinned loosely for foundation; lock with a resolver before production.
fastapi>=0.115
uvicorn>=0.30
SQLAlchemy>=2.0
pydantic>=2.7
pydantic-settings>=2.2
alembic>=1.13
# PostgreSQL/TimescaleDB driver (optional at runtime; SQLite needs no driver).
psycopg[binary]>=3.1

# ---- Dev / test (install separately or via pip install -r requirements-dev.txt) ----
# pytest>=8
# httpx>=0.27
# ruff>=0.6
