# =============================================================================
# MetoCare — CI + Staging Auto-Deploy Gate
#
# Jobs:
#   test-backend   — always runs on push (main/feature/**) + PRs to main
#   test-frontend  — always runs on push (main/feature/**) + PRs to main
#   deploy-staging — ONLY when BOTH tests pass AND push is to main
#
# Auto-deploy trigger: merge to main (push event). PRs run tests but never deploy.
# Gate: needs: [test-backend, test-backend-postgres, test-frontend, test-mobile,
#              meto-gate] + if: push to main.
#
# Deploy approach: GHCR (matches azure-staging.yml / existing infra pattern).
# Secrets already configured (azure-staging.yml uses the same set):
#   AZURE_CLIENT_ID, AZURE_TENANT_ID, AZURE_SUBSCRIPTION_ID — OIDC to Azure
#   GITHUB_TOKEN — auto-provided by GitHub for GHCR push
#
# Staging ACA vars (env-level, set in Azure):
#   RG: rg-metocare-staging
#   ENV_NAME: cae-metocare-staging
#   BACKEND_APP: ca-metocare-backend
#   FRONTEND_APP: ca-metocare-frontend
#   KV_NAME: kv-metocare-stgd9e7
#   REGISTRY: ghcr.io
# =============================================================================
name: CI + Staging Auto-Deploy

on:
  push:
    branches: [main, "feature/**", "feature/**/**"]
  pull_request:
    branches: [main]

permissions:
  id-token: write   # OIDC → Azure (needed in deploy-staging job)
  contents: read
  packages: write   # push images to GHCR

env:
  REGISTRY: ghcr.io
  RG: rg-metocare-staging
  ENV_NAME: cae-metocare-staging
  BACKEND_APP: ca-metocare-backend
  FRONTEND_APP: ca-metocare-frontend
  KV_NAME: kv-metocare-stgd9e7

# ─────────────────────────────────────────────────────────────────────────────
jobs:

  # ── 1. Backend Tests (SQLite — fast unit/API tests) ────────────────────────
  test-backend:
    name: Backend Tests
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"
          cache-dependency-path: backend/requirements-dev.txt

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-dev.txt

      - name: Ruff lint
        run: ruff check .

      # ── Alembic single-head gate (M0, program §3/§7) ────────────────────────
      # Divergent migration heads are the top program risk once parallel
      # workstreams add migrations. Fail the build unless exactly one head exists.
      - name: Alembic single-head gate
        run: |
          HEADS=$(alembic heads 2>/dev/null | grep -c '(head)' || true)
          echo "Alembic heads detected: $HEADS"
          if [ "$HEADS" -ne 1 ]; then
            echo "::error::Expected exactly 1 Alembic head, found $HEADS. Merge divergent migration heads before landing."
            alembic heads || true
            exit 1
          fi
        env:
          MCP_DATABASE_URL: ""
          ANTHROPIC_API_KEY: test-key-mock
          ENVIRONMENT: test
          MCP_ENV: test

      - name: Run backend tests
        run: python -m pytest tests/ -v --tb=short --junit-xml=test-results.xml -m "not integration"
        env:
          MCP_DATABASE_URL: ""          # empty → SQLite in-memory (established pattern)
          ANTHROPIC_API_KEY: test-key-mock  # prevents real API calls; claude_client.py checks for non-empty
          ENVIRONMENT: test
          MCP_ENV: test

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pytest-results
          path: backend/test-results.xml
        continue-on-error: true

  # ── 2. PostgreSQL Integration Tests — P0 migration gate ────────────────────
  # RISK-1 fix: SQLite cannot validate JSONB columns, CHECK constraints, or
  # FK behaviour. These tests run against a real PostgreSQL service.
  # Runs on push to main/feature/** and on PRs to main (same trigger as unit tests).
  test-backend-postgres:
    name: Backend PostgreSQL Integration Tests
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: mcp
          POSTGRES_PASSWORD: mcp_test_ci
          POSTGRES_DB: mcp_test
        ports:
          - 5432:5432
        options: >
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"
          cache-dependency-path: backend/requirements-dev.txt

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-dev.txt

      - name: Wait for PostgreSQL
        run: |
          until pg_isready -h localhost -p 5432 -U mcp; do
            echo "Waiting for PostgreSQL..."
            sleep 2
          done

      # Every file under tests/integration, each in its OWN process against its
      # OWN freshly created database.
      #
      # This step used to name five files explicitly. Eleven of the sixteen on
      # disk were therefore never run by CI at all — including the PHI-encryption
      # suite, whose entire job is to prove patient data is encrypted at rest. A
      # hand-maintained list of test files is a list that silently stops matching
      # the directory, and nothing fails when it does. Globbing removes the
      # failure mode rather than correcting one instance of it.
      #
      # Per-file databases are required, not tidiness: each of these modules runs
      # `alembic upgrade`/`downgrade` against POSTGRES_TEST_URL, so pointing them
      # all at one database makes one module's downgrade collide with another's
      # expected revision. That interference is what made the whole-suite run
      # unusable locally and is why the list existed. Isolating them fixes the
      # cause; the list only hid it.
      - name: Run PostgreSQL integration tests
        run: |
          set -uo pipefail
          files=$(ls tests/integration/test_*.py | sort)
          echo "Discovered $(echo "$files" | wc -l) integration modules:"
          echo "$files"
          rc=0
          for f in $files; do
            db="ci_$(basename "$f" .py | tr -c 'a-z0-9_' '_' | cut -c1-40)"
            echo "::group::$f  (database: $db)"
            # Check setup explicitly. `set -e` is deliberately NOT used (it would
            # abort on the first failing file instead of accumulating `rc`), so
            # without these a failed CREATE DATABASE or a failed migration fell
            # through into pytest and surfaced as a confusing connection/schema
            # error — turning a clear "the migration broke" signal into "the tests
            # broke", which is the diagnostic loss this rewrite exists to remove.
            if ! PGPASSWORD=mcp_test_ci psql -h localhost -U mcp -d postgres \
              -c "DROP DATABASE IF EXISTS $db" -c "CREATE DATABASE $db" >/dev/null; then
              echo "::error file=$f::could not create database $db"
              rc=1; echo "::endgroup::"; continue
            fi
            # Same pre-P0 baseline, so upgrade AND downgrade run from a known revision.
            if ! MCP_DATABASE_URL="postgresql+psycopg://mcp:mcp_test_ci@localhost:5432/$db" \
              alembic upgrade c0_m9_audit_log_clinic_id >/dev/null; then
              echo "::error file=$f::alembic baseline upgrade failed on $db"
              rc=1; echo "::endgroup::"; continue
            fi
            MCP_DATABASE_URL="postgresql+psycopg://mcp:mcp_test_ci@localhost:5432/$db" \
              POSTGRES_TEST_URL="postgresql+psycopg://mcp:mcp_test_ci@localhost:5432/$db" \
              python -m pytest "$f" -v --tb=short -m integration \
                --junit-xml="test-results-$(basename "$f" .py).xml"
            code=$?
            # pytest exits 5 for "no tests collected". That must FAIL here, not
            # pass quietly — it is exactly how the PHI-encryption suite hid: it
            # carried `skipif` but not `@pytest.mark.integration`, so CI-1 skipped
            # it for want of a database and CI-2 deselected it for want of the
            # marker. A suite whose job is to prove patient data is encrypted at
            # rest ran in neither, and reported green in both.
            if [ "$code" -eq 5 ]; then
              echo "::error file=$f::collected 0 tests under -m integration — missing @pytest.mark.integration?"
              rc=1
            elif [ "$code" -ne 0 ]; then
              rc=1
            fi
            PGPASSWORD=mcp_test_ci psql -h localhost -U mcp -d postgres \
              -c "DROP DATABASE IF EXISTS $db" >/dev/null
            echo "::endgroup::"
          done
          exit $rc
        env:
          # MCP_DATABASE_URL / POSTGRES_TEST_URL are set PER FILE in the loop above,
          # to that file's own database. Setting them here too would be a trap: a
          # module that reads them at import time would silently reach the shared
          # database and reintroduce the cross-module interference.
          ANTHROPIC_API_KEY: test-key-mock
          ENVIRONMENT: test
          MCP_ENV: test

      - name: Upload PostgreSQL test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pytest-results-postgres
          path: backend/test-results-*.xml  # one per integration module
        continue-on-error: true

  # ── 2. Frontend Tests ───────────────────────────────────────────────────────
  test-frontend:
    name: Frontend Tests
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: cd frontend && npm ci

      - name: Run frontend tests
        run: cd frontend && npm test -- --watchAll=false --passWithNoTests


  # ── 2b. Mobile verification (required integration gate) ────────────────────
  # Mobile was 108 of the changed paths in the Journey 2-5 candidate and was NOT
  # run by CI at all, which is why a cold-cache failure (6 of 28 suites) survived
  # undetected: jest's 5s default timeout is measured INSIDE the test body while
  # the first test pays for transforming the whole React Native + Expo Router
  # graph. CI always runs cold, so the cache is cleared here deliberately — this
  # job must exercise the cold path, not a warmed one.
  test-mobile:
    name: Mobile Tests
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: mobile/package-lock.json

      # Clean, reproducible install from the lockfile — never `npm install`.
      - name: Install dependencies (clean)
        run: cd mobile && npm ci

      - name: TypeScript
        run: cd mobile && npm run typecheck

      - name: Lint
        run: cd mobile && npm run lint

      # Cold cache on purpose: the failure this gate exists to catch only
      # reproduces on a cold transform.
      - name: Jest (cold cache)
        run: cd mobile && npx jest --clearCache && npm test -- --watchAll=false

      # Proves the app still bundles for release, not just that tests pass.
      # `--no-bytecode` keeps it dependency-free (no Hermes toolchain needed).
      - name: Production bundle export
        run: cd mobile && npx expo export --platform android --output-dir .expo-export-ci --no-bytecode

      - name: Verify the bundle was produced
        run: |
          cd mobile
          test -d .expo-export-ci || { echo "::error::expo export produced no output"; exit 1; }
          find .expo-export-ci -name '*.hbc' -o -name '*.js' | head -1 | grep -q . \
            || { echo "::error::no JS bundle in the export"; exit 1; }
          echo "bundle OK: $(du -sh .expo-export-ci | cut -f1)"

  # ── 3. Meto AI Deployment Gate ─────────────────────────────────────────────
  # Runs after unit tests pass but BEFORE deploy.
  # Gates 1-5 use mock/local only. Gates 6-7 use real keys (skipped if absent).
  meto-gate:
    name: Meto AI Deployment Gate
    runs-on: ubuntu-latest
    needs: [test-backend, test-backend-postgres, test-frontend, test-mobile]
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    outputs:
      gate_passed: ${{ steps.gate.outputs.gate_passed }}
      gate_score: ${{ steps.gate.outputs.gate_score }}
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"
          cache-dependency-path: backend/requirements-dev.txt

      - name: Install backend deps
        run: pip install -r backend/requirements-dev.txt

      - name: Run Meto Deployment Gate
        id: gate
        env:
          MCP_DATABASE_URL: "sqlite:///tmp/gate_test.sqlite3"
          MCP_AI_MODE: "mock"
          FEATURE_AI_ASSISTANT: "true"
          # 9Router is local-only (127.0.0.1:20128) — not reachable from GitHub-hosted runners.
          # Gates 6+7 (live provider ping) run in mock/simulated mode in CI.
          # Live gate validation must be run locally before deploy using: python scripts/meto_gate.py
          MCP_NINE_ROUTER_API_KEY: ${{ secrets.MCP_NINE_ROUTER_API_KEY }}
          METO_GATE_STRICT: "false"
          METO_GATE_THRESHOLD: "80"
          PYTHONPATH: backend
        run: |
          cd backend
          python scripts/meto_gate.py --ci --json > gate_report.json
          EXIT_CODE=$?
          SCORE=$(python -c "import json; d=json.load(open('gate_report.json')); print(d['score'])")
          PASSED=$(python -c "import json; d=json.load(open('gate_report.json')); print(str(d['deploy_allowed']).lower())")
          echo "gate_passed=$PASSED" >> $GITHUB_OUTPUT
          echo "gate_score=$SCORE" >> $GITHUB_OUTPUT
          cat gate_report.json
          exit $EXIT_CODE

      - name: Upload gate report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: meto-gate-report
          path: backend/gate_report.json

  # ── 4. Gate Blocked Notification ──────────────────────────────────────────
  gate-blocked:
    name: Deploy Blocked - Gate Failed
    needs: [meto-gate]
    if: >
      always() &&
      github.ref == 'refs/heads/main' &&
      github.event_name == 'push' &&
      needs.meto-gate.outputs.gate_passed == 'false'
    runs-on: ubuntu-latest
    steps:
      - name: Deploy blocked - gate failed
        run: |
          echo "DEPLOY BLOCKED"
          echo "Meto gate score: ${{ needs.meto-gate.outputs.gate_score }}/100"
          echo "Fix failing gates before deploying."
          exit 1

  # ── 5. Auto-Deploy to Staging ───────────────────────────────────────────────
  # GATE: only runs when BOTH test jobs AND meto-gate pass AND this is a push to main.
  # PRs never trigger this job (event_name == 'push' guard).
  deploy-staging:
    name: Deploy to Staging
    needs: [test-backend, test-backend-postgres, test-frontend, test-mobile, meto-gate]
    if: >
      github.ref == 'refs/heads/main' &&
      github.event_name == 'push' &&
      needs.meto-gate.outputs.gate_passed == 'true'
    runs-on: ubuntu-latest
    environment: azure-staging

    steps:
      - uses: actions/checkout@v4

      # ── Resolve image tag (SHA8 + epoch for uniqueness) ──────────────────
      - name: Resolve image tag
        id: tag
        run: |
          SHA8=$(git rev-parse --short=8 HEAD)
          EPOCH=$(date +%s)
          echo "tag=${SHA8}-${EPOCH}" >> "$GITHUB_OUTPUT"
          echo "sha8=${SHA8}" >> "$GITHUB_OUTPUT"

      # ── Azure OIDC login ──────────────────────────────────────────────────
      - name: Azure login (OIDC)
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Ensure containerapp CLI extension
        run: az extension add --name containerapp --upgrade --only-show-errors

      # ── Resolve ACA environment domain (needed to bake backend FQDN into frontend) ──
      - name: Compute Container Apps FQDNs
        run: |
          DOMAIN=$(az containerapp env show -g "$RG" -n "$ENV_NAME" \
            --query 'properties.defaultDomain' -o tsv)
          if [ -z "$DOMAIN" ]; then
            echo "::error::Could not resolve ACA env defaultDomain for $ENV_NAME — aborting"
            exit 1
          fi
          echo "BACKEND_FQDN=${BACKEND_APP}.${DOMAIN}" >> "$GITHUB_ENV"
          echo "FRONTEND_FQDN=${FRONTEND_APP}.${DOMAIN}" >> "$GITHUB_ENV"
          echo "Backend:  https://${BACKEND_APP}.${DOMAIN}"
          echo "Frontend: https://${FRONTEND_APP}.${DOMAIN}"

      # ── GHCR login ────────────────────────────────────────────────────────
      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # ── Build & push backend image ────────────────────────────────────────
      - name: Build & push backend image
        uses: docker/build-push-action@v6
        with:
          context: ./backend
          file: ./backend/Dockerfile
          push: true
          tags: ${{ env.REGISTRY }}/${{ github.repository_owner }}/metocare-backend:${{ steps.tag.outputs.tag }}

      # ── Build & push frontend image (NEXT_PUBLIC_API_URL baked at build) ──
      - name: Build & push frontend image
        uses: docker/build-push-action@v6
        with:
          context: ./frontend
          file: ./frontend/Dockerfile
          push: true
          build-args: |
            NEXT_PUBLIC_API_URL=https://${{ env.BACKEND_FQDN }}/api/v1
            NEXT_PUBLIC_BUILD_SHA=${{ steps.tag.outputs.sha8 }}
            NEXT_PUBLIC_APP_ENV=staging
          tags: ${{ env.REGISTRY }}/${{ github.repository_owner }}/metocare-frontend:${{ steps.tag.outputs.tag }}

      # ── Resume Postgres if stopped ────────────────────────────────────────
      - name: Resume Postgres if stopped
        run: |
          PG_NAME=psql-metocare-staging
          STATE=$(az postgres flexible-server show -g "$RG" -n "$PG_NAME" --query state -o tsv 2>/dev/null || echo "Unknown")
          echo "Postgres state: $STATE"
          if [ "$STATE" = "Stopped" ]; then
            az postgres flexible-server start -g "$RG" -n "$PG_NAME"
            until [ "$(az postgres flexible-server show -g "$RG" -n "$PG_NAME" --query state -o tsv)" = "Ready" ]; do
              echo "  waiting for Postgres..."; sleep 10
            done
          fi

      # ── Read secrets from Key Vault ───────────────────────────────────────
      - name: Read secrets from Key Vault
        run: |
          DB_URL=$(az keyvault secret show --vault-name "$KV_NAME" -n mcp-database-url --query value -o tsv)
          SECRET_KEY=$(az keyvault secret show --vault-name "$KV_NAME" -n mcp-secret-key --query value -o tsv)
          ENC_KEYS=$(az keyvault secret show --vault-name "$KV_NAME" -n mcp-encryption-keys --query value -o tsv)
          APPI=$(az keyvault secret show --vault-name "$KV_NAME" -n appinsights-connection-string --query value -o tsv)
          DOC_INTEL_ENDPOINT=$(az keyvault secret show --vault-name "$KV_NAME" -n azure-doc-intel-endpoint --query value -o tsv)
          DOC_INTEL_KEY=$(az keyvault secret show --vault-name "$KV_NAME" -n azure-doc-intel-key --query value -o tsv)
          DEEPSEEK_KEY=$(az keyvault secret show --vault-name "$KV_NAME" -n deepseek-api-key --query value -o tsv)
          # OpenRouter — Meto AI primary gateway (Claude Sonnet 4.5). DeepSeek stays as fallback.
          OPENROUTER_KEY=$(az keyvault secret show --vault-name "$KV_NAME" -n openrouter-api-key --query value -o tsv)
          echo "::add-mask::$DB_URL"; echo "::add-mask::$SECRET_KEY"
          echo "::add-mask::$ENC_KEYS"; echo "::add-mask::$DOC_INTEL_KEY"; echo "::add-mask::$DEEPSEEK_KEY"; echo "::add-mask::$OPENROUTER_KEY"
          {
            echo "DB_URL<<EOF"; echo "$DB_URL"; echo "EOF"
            echo "SECRET_KEY<<EOF"; echo "$SECRET_KEY"; echo "EOF"
            echo "ENC_KEYS<<EOF"; echo "$ENC_KEYS"; echo "EOF"
            echo "APPI<<EOF"; echo "$APPI"; echo "EOF"
            echo "DOC_INTEL_ENDPOINT<<EOF"; echo "$DOC_INTEL_ENDPOINT"; echo "EOF"
            echo "DOC_INTEL_KEY<<EOF"; echo "$DOC_INTEL_KEY"; echo "EOF"
            echo "DEEPSEEK_KEY<<EOF"; echo "$DEEPSEEK_KEY"; echo "EOF"
            echo "OPENROUTER_KEY<<EOF"; echo "$OPENROUTER_KEY"; echo "EOF"
          } >> "$GITHUB_ENV"

      # ── Pre-migration soft-delete audit (automated gate, replaces manual step) ─
      # DATABASE_URL must be the Key Vault mcp-database-url (same source as the
      # migration job) — the GitHub DATABASE_URL secret is not a full psql URL.
      # MEDICATION_SOFT_DELETE_MAPPINGS: per-record review approvals
      # ("<id>:<status>,..."), a SECRET so ids never reach the public log.
      # Remove the secret once the P0 migration has run.
      - name: Pre-migration soft-delete audit
        run: bash scripts/pre_migration_audit.sh
        env:
          DATABASE_URL: ${{ env.DB_URL }}
          POSTGRES_SERVER_NAME: ${{ secrets.POSTGRES_SERVER_NAME }}
          RESOURCE_GROUP: ${{ secrets.RESOURCE_GROUP }}
          MEDICATION_SOFT_DELETE_MAPPINGS: ${{ secrets.MEDICATION_SOFT_DELETE_MAPPINGS }}

      # ── Pre-migration DB backup (RISK-4 fix) ─────────────────────────────
      # ENV arg is literal "staging" — env.ENVIRONMENT is only defined in the
      # test jobs, so referencing it here expands to empty and the script's
      # usage check fails.
      - name: Pre-migration DB backup
        run: bash scripts/pre_migration_backup.sh staging $(date +%Y%m%dT%H%M%S) ${{ github.sha }}
        env:
          AZURE_CREDENTIALS: ${{ secrets.AZURE_CREDENTIALS }}
          POSTGRES_SERVER_NAME: ${{ secrets.POSTGRES_SERVER_NAME }}
          RESOURCE_GROUP: ${{ secrets.RESOURCE_GROUP }}

      # ── Run Alembic migration (one-shot Container Apps Job) ───────────────
      - name: Run Alembic migration
        run: |
          IMG="${REGISTRY}/${{ github.repository_owner }}/metocare-backend:${{ steps.tag.outputs.tag }}"
          JOB="caj-metocare-migrate"
          az containerapp job delete -g "$RG" -n "$JOB" --yes -o none 2>/dev/null || true
          az containerapp job create \
            --name "$JOB" --resource-group "$RG" --environment "$ENV_NAME" \
            --image "$IMG" \
            --trigger-type Manual \
            --replica-timeout 600 --replica-retry-limit 1 \
            --parallelism 1 --replica-completion-count 1 \
            --cpu 0.5 --memory 1.0Gi \
            --secrets "db-url=$DB_URL" \
            --env-vars "MCP_DATABASE_URL=secretref:db-url" "MCP_ENV=staging" \
            --command "alembic" --args "upgrade" "head" \
            -o none
          az containerapp job start -g "$RG" -n "$JOB" -o none
          for i in $(seq 1 40); do
            ST=$(az containerapp job execution list -g "$RG" -n "$JOB" \
              --query "[0].properties.status" -o tsv 2>/dev/null || echo Running)
            echo "  $i: $ST"
            [ "$ST" = "Succeeded" ] && break
            [ "$ST" = "Failed" ] && { echo "::error::Alembic migration failed"; exit 1; }
            sleep 10
          done

      # ── Deploy backend Container App ──────────────────────────────────────
      - name: Deploy backend to ACA
        run: |
          IMG="${REGISTRY}/${{ github.repository_owner }}/metocare-backend:${{ steps.tag.outputs.tag }}"
          COMMON_SECRETS="db-url=$DB_URL secret-key=$SECRET_KEY enc-keys=$ENC_KEYS appi=$APPI doc-intel-key=$DOC_INTEL_KEY deepseek-api-key=$DEEPSEEK_KEY openrouter-api-key=$OPENROUTER_KEY"
          COMMON_ENV="MCP_ENV=staging MCP_DEBUG=false \
            MCP_DATABASE_URL=secretref:db-url MCP_SECRET_KEY=secretref:secret-key \
            MCP_ENCRYPTION_KEYS=secretref:enc-keys \
            APPLICATIONINSIGHTS_CONNECTION_STRING=secretref:appi \
            MCP_CORS_ALLOWED_ORIGINS=https://${FRONTEND_FQDN},https://app.metocare.me \
            MCP_AI_MODE=production MCP_LLM_PROVIDER=deepseek MCP_OCR_MODE=mock \
            MCP_OCR_WORKER_ENABLED=false MCP_RAG_ENABLED=false \
            MCP_STORAGE_MODE=local MCP_RATELIMIT_BACKEND=memory \
            MCP_METRICS_ENABLED=true MCP_ENABLE_DOCS=false \
            MCP_FEATURE_OCR=true MCP_FEATURE_OCR_CLOUD_FALLBACK=true \
            MCP_OCR_CLOUD_PROVIDER=azure \
            AZURE_DOC_INTEL_ENDPOINT=${DOC_INTEL_ENDPOINT} \
            AZURE_DOC_INTEL_KEY=secretref:doc-intel-key \
            MCP_DEEPSEEK_API_KEY=secretref:deepseek-api-key \
            MCP_OPENROUTER_API_KEY=secretref:openrouter-api-key \
            MCP_OPENROUTER_PRIMARY_MODEL=anthropic/claude-sonnet-4.5 \
            MCP_OPENROUTER_FALLBACK_MODEL=openai/gpt-4o-mini \
            MCP_BUILD_SHA=${{ steps.tag.outputs.sha8 }} \
            MCP_BUILD_TIME=$(date -u +%Y-%m-%dT%H:%M:%SZ) \
            MCP_EXPECTED_HOST=${BACKEND_FQDN:-}"
          # Build identity on the AUTO-deploy path. azure-staging.yml and
          # azure-production.yml are workflow_dispatch — MANUAL — and set these;
          # this job is the one that actually deploys on merge to main and did
          # not, so every auto-deployed staging build reported an EMPTY build_sha.
          # "What is running right now?" is asked during an incident, which is
          # the worst time to discover the answer is unavailable.
          SUFFIX="be-${{ steps.tag.outputs.sha8 }}-$(date +%s)"
          if az containerapp show -g "$RG" -n "$BACKEND_APP" -o none 2>/dev/null; then
            az containerapp secret set -g "$RG" -n "$BACKEND_APP" --secrets $COMMON_SECRETS -o none
            az containerapp update -g "$RG" -n "$BACKEND_APP" --image "$IMG" \
              --revision-suffix "$SUFFIX" \
              --min-replicas 1 --set-env-vars $COMMON_ENV -o none
          else
            az containerapp create -g "$RG" -n "$BACKEND_APP" --environment "$ENV_NAME" \
              --image "$IMG" --target-port 8000 --ingress external \
              --min-replicas 1 --max-replicas 1 --cpu 0.5 --memory 1.0Gi \
              --revision-suffix "$SUFFIX" \
              --secrets $COMMON_SECRETS --env-vars $COMMON_ENV -o none
          fi

      # ── Deploy frontend Container App ─────────────────────────────────────
      - name: Deploy frontend to ACA
        run: |
          IMG="${REGISTRY}/${{ github.repository_owner }}/metocare-frontend:${{ steps.tag.outputs.tag }}"
          API_URL="https://${BACKEND_FQDN}/api/v1"
          SUFFIX="fe-${{ steps.tag.outputs.sha8 }}-$(date +%s)"
          if az containerapp show -g "$RG" -n "$FRONTEND_APP" -o none 2>/dev/null; then
            az containerapp update -g "$RG" -n "$FRONTEND_APP" --image "$IMG" \
              --revision-suffix "$SUFFIX" \
              --min-replicas 1 --set-env-vars "NEXT_PUBLIC_API_URL=$API_URL" \
                "NEXT_PUBLIC_BUILD_SHA=${{ steps.tag.outputs.sha8 }}" \
                "NEXT_PUBLIC_APP_ENV=staging" -o none
          else
            az containerapp create -g "$RG" -n "$FRONTEND_APP" --environment "$ENV_NAME" \
              --image "$IMG" --target-port 3000 --ingress external \
              --min-replicas 1 --max-replicas 1 --cpu 0.25 --memory 0.5Gi \
              --revision-suffix "$SUFFIX" \
              --env-vars "NEXT_PUBLIC_API_URL=$API_URL" \
                "NEXT_PUBLIC_BUILD_SHA=${{ steps.tag.outputs.sha8 }}" \
                "NEXT_PUBLIC_APP_ENV=staging" -o none
          fi

      # ── CORS guard — ensure app.metocare.me survives every deploy ─────────
      - name: CORS guard
        run: |
          CURRENT=$(az containerapp show -g "$RG" -n "$BACKEND_APP" \
            --query "properties.template.containers[0].env[?name=='MCP_CORS_ALLOWED_ORIGINS'].value" \
            -o tsv)
          if echo "$CURRENT" | grep -q "app.metocare.me"; then
            echo "✅ CORS OK: $CURRENT"
          else
            echo "⚠️ Patching CORS: $CURRENT"
            az containerapp update -g "$RG" -n "$BACKEND_APP" \
              --set-env-vars "MCP_CORS_ALLOWED_ORIGINS=${CURRENT},https://app.metocare.me" \
              -o none
            echo "✅ CORS patched"
          fi

      # ── Health gate — wait for backend ready ──────────────────────────────
      - name: Wait for backend healthy
        run: |
          echo "Polling https://${BACKEND_FQDN}/api/v1/health ..."
          for i in $(seq 1 18); do
            H=$(curl -s -o /dev/null -w "%{http_code}" --max-time 15 "https://${BACKEND_FQDN}/api/v1/health" || echo 000)
            echo "  $i: /api/v1/health -> $H"
            [ "$H" = "200" ] && { echo "✅ Backend healthy"; break; }
            [ "$i" = "18" ] && { echo "::error::Backend unhealthy after 3 min — ACA keeps previous healthy revision (auto-rollback)"; exit 1; }
            sleep 10
          done

      # ── Health gate — wait for frontend ready ─────────────────────────────
      - name: Wait for frontend healthy
        run: |
          echo "Polling https://${FRONTEND_FQDN}/ ..."
          for i in $(seq 1 18); do
            H=$(curl -s -o /dev/null -w "%{http_code}" --max-time 15 "https://${FRONTEND_FQDN}/" || echo 000)
            echo "  $i: / -> $H"
            [ "$H" = "200" ] && { echo "✅ Frontend healthy"; break; }
            [ "$i" = "18" ] && { echo "::error::Frontend unhealthy after 3 min"; exit 1; }
            sleep 10
          done

      # ── Smoke tests ───────────────────────────────────────────────────────
      # Routes sourced from scripts/smoke-routes.sh (auto-generated from OpenAPI).
      # To regenerate: cd backend && python3 ../scripts/generate-smoke-routes.py > ../scripts/smoke-routes.sh
      - name: Smoke tests
        run: |
          BASE_BE="https://${BACKEND_FQDN}"
          BASE_FE="https://${FRONTEND_FQDN}"
          PASS=true

          check() {
            local desc="$1" url="$2" expected="${3:-200}" method="${4:-GET}"
            local code
            code=$(curl -s -o /dev/null -w "%{http_code}" -X "$method" "$url" --max-time 30)
            if [ "$code" = "$expected" ]; then
              echo "✅ $desc: $code"
            else
              echo "❌ $desc: expected $expected got $code"
              PASS=false
            fi
          }

          # Frontend health (not in OpenAPI schema)
          check "Frontend /" "$BASE_FE/" "200" "GET"

          # Backend routes sourced from OpenAPI-generated smoke list
          # shellcheck source=scripts/smoke-routes.sh
          source scripts/smoke-routes.sh

          if $PASS; then
            echo "✅ All smoke checks passed"
          else
            echo "::error::One or more smoke tests failed — see above"
            exit 1
          fi

      # ── Post-deploy PHI crypto smoke ──────────────────────────────────────
      #
      # The checks above are UNAUTHENTICATED and assert 401s, and /health is a
      # bare SELECT 1 — none of them touches an encrypted column. Boot-time
      # validation proves the key is well-FORMED, not that it is the RIGHT key.
      #
      # Reproduced on a real database: with a wrong-but-well-formed key, boot
      # passes, /health passes, the smoke suite passes, and every authenticated
      # encrypted read raises. Reminders (Notification.title/body) and the
      # medication timeline (MedicationStatement.raw_drug_name) are NOT NULL with
      # on_decrypt_failure="raise", so a mis-rotated key takes both down for every
      # patient while the deploy is reported healthy.
      #
      # Runs as a one-shot job, not an endpoint: an HTTP route that decrypts on
      # demand is a crypto oracle. Writes synthetic CRYPTO-SMOKE sentinels only,
      # rolls them back, and prints no PHI and no key material.
      - name: Post-deploy PHI crypto smoke
        run: |
          az containerapp job create -g "$RG" -n "$MIGRATE_JOB-crypto" \
            --environment "$ACA_ENV" --trigger-type Manual --replica-timeout 300 \
            --image "$IMG" --cpu 0.5 --memory 1Gi \
            --secrets db-url="$DB_URL" enc-keys="$ENC_KEYS" \
            --env-vars MCP_DATABASE_URL=secretref:db-url \
              MCP_ENCRYPTION_KEYS=secretref:enc-keys MCP_ENV=staging \
              MCP_BUILD_SHA=${{ steps.tag.outputs.sha8 }} \
            --command "python" --args "-m" "scripts.crypto_smoke" -o none 2>/dev/null || true
          az containerapp job start -g "$RG" -n "$MIGRATE_JOB-crypto" -o none
          for i in $(seq 1 30); do
            ST=$(az containerapp job execution list -g "$RG" -n "$MIGRATE_JOB-crypto" \
              --query "[0].properties.status" -o tsv 2>/dev/null || echo Running)
            echo "  crypto-smoke $i: $ST"
            [ "$ST" = "Succeeded" ] && break
            if [ "$ST" = "Failed" ]; then
              echo "::error::PHI crypto smoke FAILED — encrypted columns are not"
              echo "::error::readable with the deployed MCP_ENCRYPTION_KEYS."
              echo "::error::Remediation: restore the previous key in Key Vault"
              echo "::error::(secret mcp-encryption-keys), redeploy, and re-run this"
              echo "::error::job. Do NOT run a downgrade — the SEC-F11/P1-5"
              echo "::error::migrations refuse to decrypt with a wrong key and will"
              echo "::error::abort rather than destroy data."
              exit 1
            fi
            sleep 10
          done
